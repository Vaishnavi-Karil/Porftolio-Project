.portfolioSection {
  padding: 50px 20px;
  /* background: #0d1117; */
}

.portfolioContainer {
  /* max-width: 1140px; */
  margin: 0 2rem;
}

.sectionTitle {
  font-size: 28px;
  font-weight: 700;
  margin-bottom: 30px;
  padding-bottom: 15px;
  border-bottom: 2px solid #2a2a3e;
  color: #fff;
}

.portfolioFilters {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 40px;
  flex-wrap: wrap;
}

.filterBtn {
  padding: 10px 26px;
  background: transparent;
  border: 2px solid rgba(20, 157, 221, 0.5);
  color: #149ddd;
  border-radius: 24px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 600;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  outline: none;
  text-transform: capitalize;
}

.filterBtn:hover {
  background: rgba(20, 157, 221, 0.1);
}

.filterBtnActive {
  padding: 10px 26px;
  background: linear-gradient(135deg, rgba(20, 157, 221, 0.9) 0%, rgba(12, 122, 181, 0.9) 100%);
  border: 2px solid rgba(125, 211, 255, 0.8);
  color: #fff;
  border-radius: 24px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 600;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  outline: none;
  text-transform: capitalize;
  box-shadow: 0 8px 24px rgba(20, 157, 221, 0.3);
}

.portfolioGrid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.portfolioCard {
  background: linear-gradient(135deg, rgba(20, 157, 221, 0.04) 0%, rgb(2 6 17 / 85%) 100%);
  /* background: linear-gradient(135deg, rgba(20, 157, 221, 0.08) 0%, rgba(22, 29, 41, 0.9) 100%); */
  border: 1px solid rgba(20, 157, 221, 0.15);
  border-radius: 14px;
  overflow: hidden;
  transition: all 0.3s ease;
}

.portfolioCard:hover {
  border-color: rgba(20, 157, 221, 0.4);
  transform: translateY(-4px);
  box-shadow: 0 12px 32px rgba(20, 157, 221, 0.15);
}

.portfolioImg {
  aspect-ratio: 16/9;
  background: linear-gradient(135deg, #149ddd 0%, #0c7ab5 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
}

.portfolioInfo {
  padding: 20px;
}

.portfolioTitle {
  display: flex;
  align-items: center;
  justify-content: space-between;
  text-decoration: none;
  color: #fff;
  margin-bottom: 12px;
}

.portfolioTitle h4 {
  font-size: 16px;
  font-weight: 600;
  margin: 0;
}

.portfolioTitle svg {
  color: #149ddd;
}

.portfolioDesc {
  color: #a8a9b4;
  font-size: 13px;
  line-height: 1.6;
  margin-bottom: 12px;
}

.techStack {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-bottom: 12px;
}

.techBadge {
  background: rgba(20, 157, 221, 0.15);
  color: #149ddd;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 500;
}

.projectHighlights {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-bottom: 16px;
}

.highlightPill {
  background: rgba(168, 169, 180, 0.1);
  color: #a8a9b4;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 11px;
}

.githubLink {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  background: #2a2a3e;
  border-radius: 50%;
  color: #149ddd;
  text-decoration: none;
  transition: all 0.3s ease;
}

.githubLink:hover {
  background: #149ddd;
  color: #fff;
}

@media (max-width: 900px) {
  .portfolioGrid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 600px) {
  .portfolioGrid {
    grid-template-columns: 1fr;
  }

  .portfolioFilters {
    gap: 8px;
  }

  .filterBtn,
  .filterBtnActive {
    padding: 8px 16px;
    font-size: 13px;
  }
}